{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "functions": {
    "api/*.ts": {
      "runtime": "edge"
    }
  },
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/api"
    }
  ]
}
